name: what_the_code_does
states:
    first:
        component: meya.text
        properties:
            text: >
                The tag loads a Javascript file from Quantcast in the background,
                without impacting site performance. The tag then collects the
                number of pageviews for that user.